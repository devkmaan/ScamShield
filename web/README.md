# ScamShield React Frontend

Separate React frontend for the ScamShield Go backend.

## Run

```powershell
npm install
npm run dev
```

The Vite dev server runs on `http://localhost:5173` and proxies API calls to the Go backend at `http://localhost:8081`.

